export const environment = {
    production: true,
    baseUrl: 'https://api-96dh.onrender.com/api' // URL para producción ejecutar (ng serve --configuration=production) para ver en producción

  };
  